name: Auto-update Just Color Picker Cask
on:
  schedule:
    - cron: "0 13 * * *" # Checks every day at 1 PM
  workflow_dispatch:

jobs:
  update-just-color-picker:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Tap
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: Run Update Script
        run: python scripts/update-just-color-picker.py

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Casks/just-color-picker.rb
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update Just Color Picker to latest version"
            git push
          fi
